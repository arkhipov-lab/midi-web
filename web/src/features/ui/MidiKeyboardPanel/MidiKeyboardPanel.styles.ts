import styled from 'styled-components'
import {Space} from 'antd'

export const MidiKeyboardPanel = styled(Space)`
  display: flex;
  gap: 1rem;
  align-items: stretch;
  width: 100%;
`

export const MidiKeyboardMidiKeyboardContainer = styled(Space)`
  flex: 1;
  min-width: 0;
`
