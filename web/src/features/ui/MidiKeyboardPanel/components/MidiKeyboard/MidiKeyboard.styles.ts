import styled from 'styled-components'
import {Space} from 'antd'

export const MidiKeyboard = styled(Space)`
  overflow-x: auto;
  overflow-y: hidden;
  border: 1px solid #d9d9d9;
  border-radius: var(--rd-sm);
  background: #fafafa;
  padding: var(--pd-sm),
`
